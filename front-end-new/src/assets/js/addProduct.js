/* eslint-disable */
function addProduct() {

    // * scroll
    const sr = ScrollReveal({
        origin: 'top',
        distance: '60px',
        duration: 1500,
        delay: 20
    })

    sr.reveal(`.add-product-container`, {distance: '20px', origin: 'bottom'})

}

export default addProduct;
